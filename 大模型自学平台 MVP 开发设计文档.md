# 大模型自学平台 MVP · 开发设计文档

> **版本**：V1.0
> **依据**：《第一性原理版｜大模型自学平台 MVP 正式PRD》+《GitHub 类似产品与技术调研》
> **原则**：所有技术决策必须能回溯到 PRD 六条公理；调研中的优质技术栈与内容源按"择优适配"原则引入，**只借技术不借模式**——不引入竞品的排名、评级、强制打卡等行业套路。

---

## 0. 文档导读

| 章节 | 内容 | 对应 PRD 章节 |
|------|------|--------------|
| 1 | 公理 → 技术决策映射表 | §1 |
| 2 | 技术栈选型（含调研择优依据） | 全部 |
| 3 | 系统架构设计 | 全部 |
| 4 | 知识库体系与内容源规划（调研择优） | §3 |
| 5 | 内容质量流水线（9条过滤规则落地） | §6 |
| 6 | 数据库设计（五集合 + 激励扩展） | §8 |
| 7 | 后端 API 设计（含测评输出定义） | §7.1 |
| 8 | 前端页面与交互设计（五大页面） | §8、§9 |
| 9 | 游戏化引擎规则（只增不减） | §4.3 |
| 10 | AI 答疑助手设计（RAG + 三重强约束） | §5 |
| 11 | 开发计划与里程碑 | §7 |
| 12 | 测试与验收标准 | §10 |
| 13 | 风险与约束 | 全部 |

---

## 1. 公理 → 技术决策映射表

每个关键技术决策都必须能回溯到一条底层公理，无法回溯的功能不予开发。

| 公理 | 推导出的技术决策 | 落实手段 |
|------|-----------------|---------|
| **公理1 内容正确性是生存底线** | 内容必须可校验、可复现、可追溯 | 内容入库流水线（§5）、代码自动运行验证、版本标注字段 |
| **公理2 筛选成本 > 理解成本** | 用户端零搜索：平台内置唯一知识源 | 知识库结构化五集合、AI 答疑强绑定知识库（RAG） |
| **公理3 持续正反馈 + 零挫败** | 激励引擎只增不减 | 积分/勋章/连续学习规则引擎（§9），数据库无负值字段 |
| **公理4 结构化 > 碎片化** | 知识树线性结构化 | 模块→章节→小节树形模型，小节强制归属体系 |
| **公理5 讨厌被考试评判** | 无锁、无评级、可跳关 | 进度判定 = 阅读完成（非做题）；跳关仅温和提示 |
| **公理6 卡点是放弃导火索** | 答疑零等待、精准绑定知识点 | RAG 答疑 + 小节级上下文注入 + 每日 20 次限流 |

---

## 2. 技术栈选型（调研择优）

### 2.1 技术栈总表

| 层面 | 选型 | 调研来源与择优理由 |
|------|------|-------------------|
| **前端框架** | Next.js 14+ (App Router) + React 18 + TypeScript | 来自 `alfredang/ai-lms`、`OpenMAIC`：React 生态成熟，SSR 首屏快（符合"极致低摩擦体验"），TypeScript 保证工程正确性 |
| **UI 组件** | Tailwind CSS + shadcn/ui | 快速构建温和、低视觉压力的界面；组件可定制，支撑"温和文案体系" |
| **后端框架** | Next.js API Routes (Server Actions) | MVP 阶段前后端一体，降低部署与维护成本；与 `ai-lms` 验证过的模式一致 |
| **数据库** | PostgreSQL + Prisma ORM | 来自 `ai-lms`：事务可靠、JSON 字段灵活；Prisma 类型安全，与五集合模型完美对应 |
| **认证** | Auth.js (NextAuth v5) + 邮箱验证码登录（Resend 发信） | PRD 闭环起点为"注册"；验证码登录提高批量注册成本，堵住限流绕过漏洞；无密码存储降低泄露风险 |
| **限流** | 用户级：DB 字段 + 惰性重置（§6.3）；IP 级：Next.js middleware 滑动窗口 | 单一数据源，避免双源冲突；双级限流防小号绕过 |
| **AI 答疑** | RAG 架构：LangChain.js + 向量数据库 | 来自 `llm-universe` 的 RAG 实践 + PRD §5 强绑定知识库要求 |
| **向量数据库** | pgvector（PostgreSQL 扩展） | MVP 阶段免引入独立中间件，降低运维成本；数据量（≤数千知识块）完全够用 |
| **LLM 供应商** | 国产模型为主（Qwen/GLM），支持多供应商切换 | 参考 `ai-lms` 的多供应商配置思路；成本可控，中文答疑质量高 |
| **代码运行验证** | CI 流水线（GitHub Actions）自动执行内容代码片段 | 落实"代码必验"准入规则（PRD §6.3），参考 `LLMs-from-scratch` 的代码可复现标准 |
| **部署** | Vercel + PostgreSQL（Neon/Supabase，启用 pgvector 扩展） | 来自 `ai-lms`：全 Serverless，零运维，成本极低 |
| **监控** | Sentry（前后端错误聚合）+ Vercel Logs | 幻觉抽检、限流异常、脚本刷分行为均需可追溯载体 |
| **内容编写** | Markdown + Frontmatter 元数据 | 来自 `llm-course`/`generative-ai-for-beginners` 的内容组织方式，Git 版本化可追溯 |

### 2.2 明确不选的技术（及原因）

| 不选 | 原因 |
|------|------|
| Frappe/LMS 全家桶 | 功能过重（证书、批次管理、支付），且带传统评分机制，违反公理3/5 |
| 独立 Redis（Upstash 等） | 答疑限流与每日重置由"DB 惰性重置 + 边缘 IP 限流"完整覆盖；引入缓存中间件会产生双数据源冲突风险 |
| LangGraph 多智能体（OpenMAIC 方案） | "AI 同学讨论"超出 MVP 边界，属于社交化设计，V1 砍掉 |
| 独立向量数据库（Milvus/FAISS） | MVP 知识块规模小，pgvector 足够，避免运维复杂度 |
| 在线代码沙盒 | PRD §7.2 明确不做 |

---

## 3. 系统架构设计

```
┌─────────────────────────────────────────────────────────┐
│                     前端 (Next.js)                        │
│  首页 │ 测评页 │ 学习总览页 │ 小节学习页 │ 个人中心          │
└──────────────────────┬──────────────────────────────────┘
                       │ REST API / Server Actions
┌──────────────────────▼──────────────────────────────────┐
│                   后端 (Next.js API)                     │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐  │
│ │ 内容服务  │ │ 进度服务  │ │ 激励引擎  │ │ AI答疑服务   │  │
│ └──────────┘ └──────────┘ └──────────┘ └──────┬──────┘  │
└────────┬─────────────┬────────────┬───────────┼─────────┘
         │             │            │           │
┌────────▼───┐ ┌───────▼────┐ ┌────▼──────┐ ┌────▼──────────┐
│ PostgreSQL │ │  pgvector  │ │ 边缘 IP   │ │ LLM API       │
│(五集合+激励)│ │ (知识块嵌入)│ │ 限流兜底   │ │ (Qwen/GLM…)  │
└────────────┘ └────────────┘ └───────────┘ └───────────────┘

┌─────────────────────────────────────────────────────────┐
│              离线流水线：内容生产与质量保障                  │
│  AI初稿 → 9条过滤规则 → 人工校对 → CI代码验证 → 习题编写    │
│  （Markdown仓库 + GitHub Actions，独立于线上系统）          │
└─────────────────────────────────────────────────────────┘
```

**架构原则**：
1. 单体优先：MVP 阶段一个 Next.js 应用承载全部在线功能，符合"最小集合"裁剪公理
2. 内容生产与线上服务解耦：内容走 Git 流水线（可审查、可回滚），审核通过后同步进数据库
3. AI 答疑为独立服务模块，失败不影响核心学习流程（降级为"暂不可用"温和提示）

---

## 4. 知识库体系与内容源规划（调研择优）

### 4.1 知识树结构（对齐 PRD §3.2）

```
大模型工程能力知识树（V1 固定结构）
├── 模块1 前置基础
│   ├── Python 工程最小子集（够用原则）
│   ├── 机器学习基础概念
│   ├── 深度学习基础概念
│   └── RNN/LSTM 与传统 NLP 对比（了解级）
├── 模块2 大模型核心原理
│   ├── Transformer 架构 / 注意力机制
│   ├── Tokenizer / 预训练原理
│   ├── 采样策略 / 幻觉成因
│   └── RLHF / DPO 对齐原理
├── 模块3 应用层
│   ├── Prompt 工程
│   ├── RAG 全链路
│   ├── Agent 机制
│   └── 多模态基础
├── 模块4 工程实战层
│   ├── 模型部署 / LoRA·QLoRA 微调
│   ├── 向量库使用 / API 开发
│   └── 简单 Agent 项目
├── [可选包] 零基础预习包（数学最小集 + Python 补强）
└── [可选包] 高阶研究包（V1 仅 UI 占位，不上内容）
```

### 4.2 内容创作源（调研择优 + 严格校验）

**重要约束（公理1）**：以下内容源仅作为**创作参考与事实校验依据**，所有入库内容为平台原创重写，禁止直接搬运；每条知识点必须经人工终审。

| 知识模块 | 首选参考源 | 择优理由（来自调研） |
|---------|-----------|---------------------|
| Transformer/注意力/Tokenizer | [rasbt/LLMs-from-scratch](https://github.com/rasbt/LLMs-from-scratch)（97k★） | 从零实现、逐组件拆解、代码可复现，概念拆解粒度与本平台"5–10分钟小节"天然匹配 |
| 预训练/微调/RLHF/DPO 原理 | [mlabonne/llm-course](https://github.com/mlabonne/llm-course)（71k★） | 三层路径覆盖科学家视角，DPO/量化/评估章节质量公认最高 |
| Prompt 工程 / RAG / Agent | [microsoft/generative-ai-for-beginners](https://github.com/microsoft/generative-ai-for-beginners)（117k★） | 微软官方持续迭代，概念+构建双轨，负责任的 AI 与安全防护内容可直接对标"内容边界" |
| 模型部署 / LoRA·QLoRA 微调 / 向量库 | [datawhalechina/self-llm](https://github.com/datawhalechina/self-llm)（31k★） | 国内模型覆盖最全、命令可直接复现，是"代码必验"准入标准下最可靠的中文实操依据 |
| RAG 应用项目实战 | [datawhalechina/llm-universe](https://github.com/datawhalechina/llm-universe)（8.8k★） | 项目驱动（知识库助手），删去底层冗余，与模块4"简单 Agent 项目"定位一致 |
| 前置基础（Python/ML/DL） | 平台原创 + 官方文档（PyTorch/HuggingFace） | 前置基础为"够用原则"最小子集，直接对齐官方文档避免二手转述失真 |

### 4.3 小节内容标准模板（对齐 PRD §3.3）

每个小节（5–10 分钟阅读量）必须包含以下结构（Markdown + Frontmatter）：

```markdown
---
sectionId: m2-ch3-s1
title: "注意力机制：为什么模型能'记住'上下文"
difficulty: core            # core | prep | advanced
estimatedMinutes: 8
version: "2026-08"          # 版本标注（公理1：版本明确）
sources: ["LLMs-from-scratch/ch03"]   # 参考源溯源字段
codeVerified: true          # CI 验证通过标记
reviewedBy: "reviewer-01"   # 人工终审记录
lastReviewedAt: "2026-08-20"
---

## 一句话直觉（先通俗）
<!-- 拒绝公式堆砌，先用类比讲清楚 -->

## 核心讲解（后专业）
<!-- 概念准确、无过时内容 -->

## 可选代码片段
<!-- 本地验证通过 + 依赖版本标注 -->

## 随堂轻习题（2–4 道）
<!-- 对错不奖不罚，提交即 +5 分 -->
```

---

## 5. 内容质量流水线（9 条过滤规则落地）

对齐 PRD §6"质量保障护城河"，流水线为：**AI初稿 → 9条过滤规则 → 人工校对 → 代码全量运行验证 → 习题人工编写 → 上线**。

### 5.1 九条硬性去噪规则

| # | 规则 | 执行方式 |
|---|------|---------|
| 1 | 概念准确性：与 ≥2 个权威源交叉验证 | 人工 + 参考源清单（§4.2） |
| 2 | 时效性：技术版本 ≤12 个月，过时技术直接剔除 | 版本字段 + 人工判定 |
| 3 | 代码可运行：**分级验证**——A级（CI 可运行）必过 CI；B级（需 GPU/API Key，如部署、微调）在 AutoDL 人工验证并存档日志；C级（配置/部署命令）静态检查 | GitHub Actions + AutoDL 验证记录存档 |
| 4 | 无营销水文：检测引流/广告/夸大表述 | AI 检测 + 人工复核 |
| 5 | 无偏方技巧：每个实践建议须有工业界依据 | 人工审查来源 |
| 6 | 无越界内容：对照永久剔除清单（CV、无关 AI） | 边界清单比对 |
| 7 | 无幻觉：AI 生成内容 100% 人工终审 | 强制人工签名（reviewedBy） |
| 8 | 难度适配：小节阅读量 5–10 分钟，超出即拆分 | 字数检查 + 人工拆分 |
| 9 | 文案合规：无打击式/否定式/评判式表述 | 文案规范扫描（§8.4） |

### 5.2 流水线工程实现

```
content-repo (Git 仓库，Markdown + 代码片段)
   │  ① AI 初稿生成（含来源引用要求）
   │  ② 提交 PR 触发 GitHub Actions：
   │     ├─ 规则4/6/8/9：自动化静态检查脚本
   │     ├─ 规则3-A：CI 可运行代码块 → 安装依赖 → 执行 → 输出结果
   │     ├─ 规则3-C：配置/部署命令 → 静态检查（版本锁定、无敏感信息）
   │     └─ 生成检查报告（B级代码必须附 AutoDL 验证日志，否则门禁拒绝合并）
   │  ③ 人工校对（规则1/2/5/7）+ 习题三项终审（题干正确性、答案唯一性、解析温和性），在 PR 中分别签名
   │  ④ 合并后自动同步脚本：Markdown → 解析 → 写入数据库五集合（小节相关数据全量重灌）
   │  ⑤ 知识块嵌入：删除该小节旧块 → 重新分块 → Embedding → pgvector（原子替换，防旧向量致幻觉）
   │
   └─ 定时回归：A级代码块每周定时任务重跑，失败自动创建内容工单，
      并将对应小节 codeVerified 置为 false（下架代码展示直至复检）
```

---

## 6. 数据库设计（五集合 + 激励扩展）

### 6.1 五集合主模型（对齐 PRD §8）

```prisma
// 集合1：模块
model LlmModule {
  id          String   @id @default(cuid())
  code        String   @unique        // "prep" | "core_principle" | "app" | "engineering"
  name        String
  description String
  isOptional  Boolean  @default(false) // 可选包标记（预习包/高阶包）
  order       Int
  chapters    LlmChapter[]
}

// 集合2：章节
model LlmChapter {
  id       String   @id @default(cuid())
  moduleId String
  module   LlmModule @relation(fields: [moduleId], references: [id])
  name     String
  order    Int
  sections LlmSection[]
}

// 集合3：小节
model LlmSection {
  id                 String   @id @default(cuid())
  chapterId          String
  chapter            LlmChapter @relation(fields: [chapterId], references: [id])
  title              String
  bodyMarkdown       String          // 正文
  codeSnippets       Json?           // 可选代码片段（含版本标注）
  estimatedMinutes   Int              // 5–10
  version            String           // 内容版本标注
  sourceRefs         String[]         // 参考源溯源
  order              Int
  exercises          LlmExercise[]
}

// 集合4：习题
model LlmExercise {
  id          String   @id @default(cuid())
  sectionId   String
  section     LlmSection @relation(fields: [sectionId], references: [id])
  question    String
  type        String                  // "single" | "multi" | "judge"
  options     Json
  answer      Json                    // 仅服务端持有，前端不返回
  explanation String                  // 温和解析文案（错题用）
  knowledgePoint String               // 知识点标签（错题一键回看用）
  order       Int
}

// 集合5：用户进度
model UserProgress {
  id              String   @id @default(cuid())
  userId          String
  sectionId       String
  completed       Boolean  @default(false)   // 阅读完正文即完成
  completedAt     DateTime?
  // 习题对错不影响 completed —— 公理5
  @@unique([userId, sectionId])
}
```

### 6.2 激励扩展表（只增不减原则的数据层保证）

```prisma
model User {
  id            String   @id @default(cuid())
  email         String   @unique        // Auth.js 邮箱验证码登录（无密码存储）
  nickname      String
  points        Int      @default(0)    // 积分：只有加法操作
  streakCurrent Int      @default(0)    // 当前连续天数
  streakBest    Int      @default(0)    // 历史最高：永不下降
  lastActiveDay DateTime?
  badges        UserBadge[]
  wrongBooks    WrongBook[]
  qaRemainToday Int      @default(20)   // 每日答疑余量（触发器重置）
  qaResetDate   DateTime
  prepPackOn    Boolean  @default(false) // 预习包开关（仅控展示）
  advPackOn     Boolean  @default(false) // 高阶包开关（仅控展示）
  assessment    Json?                   // 测评结果（仅偏好分流，无分数）
}

model UserBadge {           // 勋章：一旦插入永不删除（无 delete API）
  id        String   @id @default(cuid())
  userId    String
  badgeCode String         // 进度类/行为类/探索类
  earnedAt  DateTime @default(now())
  @@unique([userId, badgeCode])
}

model PointsLog {           // 积分流水：type 只有加分类型，幂等防并发重复加分
  id     String @id @default(cuid())
  userId String
  dayKey String               // "2026-08-27"（北京时间），用于每日去重/封顶
  delta  Int                 // 恒为正数（应用层校验）
  type   String              // SECTION_DONE|EXERCISE_SUBMIT|CODE_PRACTICE|FEEDBACK
  refId  String?
  createdAt DateTime @default(now())
  @@unique([userId, type, refId, dayKey])  // 同用户+同类型+同对象+同日只加一次分，竞态下幂等
  @@index([userId, dayKey])
}

model DailyPointCap {       // 每日分源封顶表，防刷分（见 §9.1 封顶规则）
  id      String @id @default(cuid())
  userId  String
  dayKey  String
  source  String             // EXERCISE_SUBMIT | FEEDBACK | TOTAL
  points  Int    @default(0)
  @@unique([userId, dayKey, source])
}

// 扩展集合：知识块（AI 答疑 RAG 用，非五集合但必须与内容同步）
model KnowledgeChunk {
  id        String @id @default(cuid())
  sectionId String
  body      String
  // embedding vector(1024) —— pgvector，选 bge-m3（中文效果好、1024维）
  version   String           // 与小节 version 一致；内容更新必须删旧块重建（§5.2 步骤⑤）
  @@index([sectionId])
}

model WrongBook {           // 错题本：无限重做，记录最后状态与是否征服
  id         String @id @default(cuid())
  userId     String
  exerciseId String
  attempts   Int    @default(1)
  lastResult Boolean
  conquered  Boolean @default(false)  // 曾做错后重做答对即置 true，永不回退；"错题征服者"勋章据此判定
  @@unique([userId, exerciseId])
}

model Feedback {            // 内容反馈闭环（管理端确认有效后 +5 分，防垃圾反馈刷分）
  id        String @id @default(cuid())
  userId    String
  sectionId String
  content   String
  status    String @default("open")   // open | accepted（确认有效，发分）| fixed | rejected
  createdAt DateTime @default(now())
}
```

### 6.3 每日事件：统一惰性重置（无 cron 依赖，Serverless 友好）

平台仅有**答疑重置**与**连续学习更新**两个日期相关事件，均在触发点惰性计算，无定时任务。统一时区：**北京时间（Asia/Shanghai）**。

**答疑次数重置**（对齐 PRD §8 唯一触发器）：

```
用户请求答疑时：
  if (user.qaResetDate < 今天 0 点) {
      user.qaRemainToday = 20
      user.qaResetDate = 今天
  }
  if (user.qaRemainToday <= 0) → 返回温和拦截提示（不消耗、不报错）
  else user.qaRemainToday -= 1
```

**连续学习更新**（任意学习动作 + 打开总览页时触发）：

```
today = 当前北京时间日期（取日，不含时分秒）
if user.lastActiveDay == today:
    不处理（当日重复活跃不重复计数）
else if user.lastActiveDay == today - 1 天:
    streakCurrent += 1
    streakBest = max(streakBest, streakCurrent)   // 只升不降，永不回退
else:                                             // 断签场景（含首次学习）
    streakCurrent = 1                             // 仅重置当前值；无扣分、无通知、无打击文案；streakBest 不变，前端只强调"最高纪录"
user.lastActiveDay = today
```

边界约定：用户 23:59 学习、次日 00:01 再学习 → 计为连续两天活跃（不断签）。文案中禁止出现"零点清零"类表述。

---

## 7. 后端 API 设计

### 7.1 核心接口清单

| 模块 | 接口 | 说明 |
|------|------|------|
| 测评 | `POST /api/assessment` | 提交 5 题测评，返回学习路径建议（无分数、无评级） |
| 内容 | `GET /api/tree` | 获取知识树（按可选包开关过滤展示） |
| 内容 | `GET /api/sections/:id` | 小节详情（正文+代码+习题题干，不含答案） |
| 进度 | `POST /api/progress/:sectionId/complete` | 阅读完成 → 置完成 +10 分 + 徽章检查 |
| 习题 | `POST /api/exercises/:id/submit` | 提交习题（对错均 +5，同题每日仅首次计1次分）；对错均返回解析；错 → 额外写入错题本 |
| 习题 | `POST /api/exercises/:id/retry` | 重做（无限次，不惩罚；不再重复计分） |
| 激励 | `GET /api/me/growth` | 积分、勋章、连续天数、统计 |
| 答疑 | `POST /api/qa` | 限流检查（§6.3）→ RAG 检索 → 生成答疑（携带小节上下文） |
| 反馈 | `POST /api/feedback` | 内容 BUG 反馈（管理端确认有效后 +5 分） |
| 管理 | `GET /api/admin/feedback` · `PATCH /api/admin/feedback/:id` | 管理端查看/处理反馈（状态流转 open→accepted→fixed；accepted 时发分） |
| 设置 | `PATCH /api/me/packs` | 可选包开关（仅改展示标志，不动进度） |

**接口安全基线**：
- 鉴权：除认证相关（发验证码/登录）与知识树静态缓存外，全部接口要求登录会话；
- 限流：边缘 middleware 按 IP 滑动窗口限流（未登录 60 次/分钟，已登录 300 次/分钟），防暴力提交习题与接口探测；
- 习题答案、答疑剩余次数仅服务端持有，永不下发前端。

### 7.2 关键业务规则（服务端强制）

1. **积分只增**：所有 `PointsLog.delta` 必须 > 0，应用层硬校验，无扣分代码路径存在。
2. **积分幂等与封顶**：任何加分动作在单个事务内完成：唯一约束幂等检查（同用户+同类型+同对象+同日只加一次）→ 每日封顶检查（`DailyPointCap`，见 §9.1）→ 加分 → 徽章检查；并发重复提交不产生双倍积分。
3. **完成判定**：普通小节 = 阅读上报通过停留时长校验（见 §8.2）；代码实操小节 = 阅读上报 + 全部代码块展开 + 提交 1 道代码理解题。V1 无代码沙盒，此为唯一可验证动作集。
4. **脚本刷分防御**：完成上报必须携带前端累计停留时长，服务端校验低于 `estimatedMinutes × 60 × 30%` 秒则拒绝加分，返回"不急，慢慢看～"温和提示（不计为失败，可再次上报）。
5. **跳关**：前端自由跳转，后端不做任何拦截（仅前端温和提示一次）。
6. **断签**：`streakCurrent` 归零，但 `streakBest` 只在其更大时更新，永不回退；判定公式见 §6.3。
7. **习题判分**：答案永不随题干下发；多选题按"漏选也算错但不惩罚"判分；解析对错均展示（首次答对也展示，强化沉淀）。
8. **反馈发分**：管理端确认 `accepted` 时才发放 +5，防垃圾反馈刷分；状态变更后用户在个人中心可见。
9. **可选包开关**：仅改展示标志位，用户已有进度数据永不删除、永不重置。
10. **新注册账号答疑限额**：注册当日答疑上限 5 次（防批量注册薅 Token），次日起恢复 20 次。
11. **时区统一**：所有"日"的概念均为北京时间（Asia/Shanghai），包括 `dayKey`、`lastActiveDay`、`qaResetDate`。

### 7.3 测评输出与学习路径定义（补齐数据载体）

`User.assessment` 为结构化 JSON（非自由格式）：

```json
{
  "pythonLevel": "none | basic | proficient",
  "goal": "principle | application | engineering",
  "startSectionHints": { "prep": "m1-ch1-s1", "core": "m2-ch1-s2" },
  "suggestPrepPack": true
}
```

路径消费规则（对齐公理5）：
- 测评输出**仅作建议**，在总览页渲染为"推荐路径"（知识树默认排序/高亮），不产生任何硬锁；
- 用户可自由跳转任意小节，推荐路径不拦截、不变化；
- `suggestPrepPack=true` 时，测评结果页默认勾选预习包开关，用户可一键修改；
- 重新测评覆盖旧结果，但**不影响**任何已完成进度与已获积分/勋章。

---

## 8. 前端页面与交互设计（五大页面）

### 8.1 页面职责

| 页面 | 路由 | 核心职责 |
|------|------|---------|
| 首页 | `/` | 新用户 → 引导测评；老用户 → 直达学习总览（记住状态，零重复摩擦） |
| 极速测评页 | `/assessment` | 5 题、无打分、无评级、文案全部为偏好询问式（"你更想……"） |
| 学习总览页 | `/overview` | 能力图谱、可选包双开关、树形目录、学习统计（只展示增长数据） |
| 小节学习页 | `/learn/[sectionId]` | 正文 + 代码块（只读高亮，不提供沙盒）+ 随堂习题 + 答疑入口 + 反馈入口 + 完成弹窗 |
| 个人中心 | `/me` | 勋章墙、积分、统计、错题本、可选包开关、重新测评、反馈记录状态 |

> 五大核心页面之外，设**极简管理端**为独立受保护路由（`/admin`，仅管理员角色可访问，不计入五大核心页面），用于处理内容反馈闭环（见 §7.1 管理接口）

### 8.2 关键交互规则

- **小节完成判定**：阅读完成 = 滚动到达正文末尾 且 累计停留时长 ≥ estimatedMinutes×30%；前端埋点上报、服务端复核（§7.2 规则4），未达标返回"不急，慢慢看～"温和提示且不加分；达标后弹出温和完成弹窗（+10 分动效），习题可跳过。代码实操小节额外要求：展开全部代码块 + 提交 1 道代码理解题（+15）
- **习题答错**：文案模板 = 肯定尝试 + 知识点解析 + 一键回看小节，**无任何"错误/失败/扣分"字样**；答对时同样展示解析（强化沉淀，首次答对不视为惩罚缺失）
- **跳关提示**：仅温和气泡"你也可以先学 XX 小节～"，不拦截、不锁进度
- **答疑用尽**：温和提示"今天的答疑次数用完啦，明天再来～"，不弹报错
- **可选包开关**：切换仅过滤展示列表；已产生进度数据永不删除

### 8.3 能力图谱

学习总览页展示各模块完成度（环形/树形图），**只展示已学数量与百分比，不展示排名、不展示落后提示**。

### 8.4 文案规范（体验准则落地，自动化扫描项）

| 禁用词/模式 | 替代模式 |
|------------|---------|
| 错误、失败、答错了 | "再试一次"、"这个思路可以换个角度" |
| 扣分、惩罚、清零 | （永不出现） |
| 排名、落后、超过 X% 的人 | （永不出现） |
| 必须、强制、不可跳过 | "建议"、"你也可以" |

---

## 9. 游戏化引擎规则（只增不减）

> 借鉴 `frappe/lms` 的勋章事件驱动架构思想，但**剔除其评分/证书/竞争元素**。

### 9.1 积分规则（对齐 PRD §5.1）

| 事件 | 积分 | 触发点 |
|------|------|--------|
| 完成小节 | +10 | 阅读完成上报 |
| 提交习题（无论对错） | +5 | 每次提交（同题每日封顶，防刷） |
| 完成代码实操小节 | +15 | 小节类型为 code_practice |
| 内容反馈 | +5 | 反馈经管理端确认有效时发放（防垃圾反馈刷分） |

积分用途：主题皮肤、拓展资料解锁；**永不影响核心课程可访问性**。

每日封顶（防刷分，不影响学习体验本身）：习题积分 ≤30/日；反馈积分 ≤15/日；单人积分总量 ≤200/日。超出部分动作仍正常记录进度/错题本，仅不计分。

PRD 解释基线："习题可无限重做"保护的是学习体验（重做次数不限、不惩罚）；积分按同题每日至多计 1 次。二者不冲突，作为验收基线。

### 9.2 勋章规则（永久获得，永不回收）

| 类别 | 示例 | 触发条件 |
|------|------|---------|
| 进度类 | 启程者 / 模块完成 / 主干毕业 | 首个小节完成 / 模块 100% |
| 行为类 | 七日同行 / 连续学习里程碑 | 连续天数达标（用 `streakCurrent` 或 `streakBest` 任一触发即永久获得） |
| 探索类 | 好奇心 / 反馈达人 / 错题征服者 | 完成可选包小节 / 反馈被确认有效 / 错题本 conquered=true 达标 |

实现：事件驱动——各业务动作完成后调用 `badgeEngine.check(userId, event)`，幂等写入（已拥有则跳过）。

### 9.3 连续学习（防打击）

- `streakCurrent`：断签归零（惰性计算，无"打卡"概念，任何学习动作即算活跃；时区与判定公式见 §6.3）
- `streakBest`：永久保留，只升不降
- 前端永远同时展示两者，文案强调"最高纪录"而非"中断"

---

## 10. AI 答疑助手设计（三重强约束）

对齐 PRD §5 与公理6，技术路线参考调研中 `llm-universe` 的 RAG 实践，但做**更强的边界约束**。

### 10.1 架构流程

```
用户提问（当前小节 ID 随请求携带）
  │
  ├─ ① 限流检查（用户级：DB 惰性重置，见 §6.3；边缘 IP 限流兜底。余量 ≤ 0 → 温和拦截，不调用 LLM，不消耗 Token）
  │
  ├─ ② 上下文构建
  │     ├─ 当前小节全文（强注入，最高优先级）
  │     ├─ 同章节相关小节摘要
  │     └─ pgvector 向量检索 Top-3 知识块（相似度阈值过滤）
  │     【预算】注入总量 ≤4K tokens；超出时按优先级裁剪：
  │       先裁向量知识块 → 再裁章节摘要 → 小节全文至少保留 70%
  │
  ├─ ③ LLM 生成（系统提示词含全部禁止规则）
  │
  └─ ④ 输出（无来源引用时附加"该内容超出当前小节范围"提示）
```

### 10.2 系统提示词核心规则（禁止范围工程化）

1. **只答知识库内容**：上下文未覆盖 → 回答"这部分我暂时无法确认，建议先学习 XX 小节"，禁止编造
2. **不闲聊**：非学习问题 → 温和引导回正题
3. **不越阶**：检测到高阶主题 → 引导开启高阶包，不直接讲解
4. **不输出完整大型项目代码**：代码回复仅限片段级解释与纠错
5. **能力范围**：通俗重讲、代码纠错、习题解析、知识点对比

### 10.3 成本控制

- 每日 20 次硬限流（用户级），限流判断在 LLM 调用**之前**执行
- 上下文窗口裁剪：注入内容总量 ≤ 4K tokens
- 模型降级策略：高峰时段自动切换至轻量模型；叠加 §7.2 规则10：新注册账号当日限 5 次，防批量注册薅 Token。

### 10.4 阈值校准与监控（防"过度拒答"与"幻觉"两种失效）

- 相似度阈值初始取 **0.65**，上线前用 ≥100 条标注问答集（含"应回答"与"应拒答"两类）校准，兼顾精确率与召回率；
- 监控指标：拒答率（目标 < 15%）、幻觉抽检合格率（目标 ≤2% 幻觉率，周频）、日均 Token 消耗；
- 拒答率过高 → 下调阈值 + 加大章节上下文注入；发现幻觉 → 追溯到具体 chunk 修正内容或上调阈值，每次事故可归因、可归档（Sentry 标签）。

---

## 11. 开发计划与里程碑

| 阶段 | 周期 | 交付物 | 验收点 |
|------|------|--------|--------|
| **P0 基建** | 第 1 周 | 项目脚手架、数据库建模、CI 代码验证流水线 | 五集合可迁移；代码验证流水线跑通 |
| **P1 内容流水线** | 第 1–3 周（并行） | 9 条过滤规则落地、首批 40 个小节产出 | 40 节通过全部质量门禁 |
| **P2 学习闭环** | 第 2–4 周 | 认证注册、测评页、知识树、小节学习页、进度与习题 | 注册→测评→学习→做题全链路通顺 |
| **P3 激励引擎** | 第 4–5 周 | 积分/勋章/连续学习/错题本/个人中心 | 只增不减规则全覆盖测试 |
| **P4 AI 答疑** | 第 5–6 周 | RAG 管线、限流、禁止规则、极简管理端 | 200 条对抗样本幻觉率 ≤2%；反馈闭环可用 |
| **P5 打磨上线** | 第 7 周 | 文案规范扫描、安全抽检（脚本刷分/限流绕过）、部署 | 达到 §12 验收标准 |

---

## 12. 测试与验收标准（对齐 PRD §10 三条终极判定）

### 12.1 功能验收

- [ ] 完整闭环：注册 → 测评 → 学习 → 做题 → 答疑 → 成长沉淀，链路无断点。
- [ ] 认证会话：邮箱验证码注册/登录，会话持久化，回访直达总览页，断网重进不丢进度。
- [ ] 防刷测试：脚本批量完成小节/恶意重复提交/垃圾反馈刷分，无效积分 = 0，进度不可伪造。
- [ ] 40 个高质量小节全部通过 9 条质量门禁，代码片段 100% 可运行
- [ ] 积分/勋章/连续学习：遍历全部规则，**无任何负向变动路径**
- [ ] AI 答疑：200 条对抗样本（知识库外/闲聊/越阶/索要完整项目代码）抽检，幻觉率 ≤ 2%，每条幻觉可归因；第 21 次提问准确拦截且不消耗 Token。
- [ ] 内容反馈：提交 → 管理端可见 → 确认有效发分 → 修复后用户可见状态变更。

### 12.2 体验验收（公理级测试）

- [ ] 全站文案扫描：零打击词、零惩罚词、零排名词
- [ ] 跳关零拦截：任意小节直达，仅温和提示
- [ ] 断签测试：`streakBest` 不回退，勋章不回收，积分不减少
- [ ] 错题重做：无限次、无惩罚、解析温和。
- [ ] 连续学习边界：23:59→00:01 等跨天场景按 §6.3 公式判定正确；`streakBest` 在任何场景下不回退。

### 12.3 终极判定（PRD §10 三条）

1. **信息问题解决**：用户无需外网搜资料，平台内容正确、完整、可落地
2. **留存问题解决**：全程无压力、高频正反馈、无挫败、可持续
3. **学习闭环完整**：入门学习 → 练习 → 答疑 → 成长沉淀完全通顺

---

## 13. 风险与约束

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 内容产能不足（40 节质量门禁高） | 延期 | 内容流水线与开发并行（P1）；优先主干模块 2/3 |
| AI 答疑幻觉漏网 | 违反公理1 | 相似度阈值 + 无来源即拒答 + 上线前抽检 + 用户反馈兜底 |
| LLM API 成本超预算 | 运营风险 | 限流前置、上下文裁剪、轻量模型降级 |
| 技术过时（大模型迭代快） | 内容失效 | 版本字段 + 12 个月时效规则 + 季度内容复审计划 |
| Serverless 冷启动影响答疑体验 | 体验下降 | 答疑异步流式输出；核心读路径走 CDN 静态缓存 |
| 批量注册绕过答疑限流 | 成本超支 | 邮箱验证码注册 + 边缘 IP 限流 + 新账号当日限 5 次（§7.2 规则10） |
| 内容代码发布后因依赖升级失效 | 违反公理1 | A级代码每周定时 CI 回归，失败自动建工单并下架代码展示（§5.2） |
| Embedding/LLM 模型更换 | 答疑质量波动 | KnowledgeChunk 记录模型与版本；更换前重跑标注集校准并抽检 |
| 40 节产能与质量冲突 | 延期或质量下滑 | 需 ≥2 名内容审校人力；人力不足时先收敛为主干模块2/3 的 20 节，其余延后 |
| 认证会话泄露/账号盗用 | 用户数据受损 | 无密码设计（验证码登录）+ HttpOnly 会话 Cookie + Sentry 异常登录告警 |

**合规与密钥底线（MVP 最小说明）**：
- LLM API Key 等密钥仅存于 Vercel 环境变量，永不进入内容仓库与前端打包产物；
- 用户可申请删除账号与全部学习数据（硬删除：进度、积分、勋章、错题本、反馈）；
- 不采集闭环外的行为数据，不引入第三方行为分析 SDK。

---

## 附录 A：调研成果引用索引

本开发文档从《GitHub 类似产品与技术调研》中择优采纳的内容：

| 调研项目 | 采纳内容 | 用于本文档 |
|---------|---------|-----------|
| rasbt/LLMs-from-scratch | 内容创作参考源（原理层）+ 代码可复现标准 | §4.2、§5.1 |
| mlabonne/llm-course | 内容创作参考源（微调/对齐层）+ 三层路径思想 | §4.1、§4.2 |
| microsoft/generative-ai-for-beginners | 内容创作参考源（应用层）+ 概念/构建双轨制 | §4.2、§4.3 |
| datawhalechina/self-llm | 内容创作参考源（工程实战层） | §4.2 |
| datawhalechina/llm-universe | RAG 架构实践经验 | §10 |
| THU-MAIC/OpenMAIC | 教育理论参考（即时反馈）；多智能体方案**不采纳**（超出 MVP） | §13 |
| frappe/lms | 勋章事件驱动架构思想；评分/证书体系**不采纳**（违反公理） | §9.2 |
| alfredang/ai-lms | Next.js + Prisma + 多模型 AI 技术栈组合验证 | §2.1 |

## 附录 B：明确不做清单（再次确认，防止范围蔓延）

- 在线代码运行沙盒 / AI 简答题批改
- 社群、评论、问答社区、任何社交功能
- 付费体系、等级、排名、内卷机制
- 移动端适配、高阶包完整内容
- 证书颁发、批次管理（传统 LMS 套路）

> （注：部分内容可能由 AI 生成）
