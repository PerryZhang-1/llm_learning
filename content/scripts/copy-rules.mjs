/**
 * 内容质量词表 —— 全局唯一权威（开发文档 §5.1 规则 4/6/9 + §8.4 文案规范）。
 *
 * 使用方：
 *  - content-lint.mjs（内容流水线 CI，规则 4/6/9）
 *  - R4 的前端文案扫描 copy-scan（UI 字符串，规则 9 全量档）
 *
 * 分层说明（对 §8.4 的一处有意裁剪，勿"修复"）：
 * §8.4 把「错误/失败/必须」列入禁用词，但其立法本意是禁止"打击式表述"（答错了→再试一次）。
 * 教学正文必须能讨论"幻觉导致错误答案""请求失败重试"这类事实概念，一刀切会让规则不可用。
 * 因此：
 *  - JUDGMENT_TERMS（评判/惩罚/竞争/施压）→ 任何位置出现即 error
 *  - CAUTION_TERMS（错误/失败/必须）→ warning（人工复核），前端 UI 字符串扫描时应升格为 error
 */

/** 评判/惩罚/竞争/施压类：全位置 error（§8.4 硬禁项主体） */
export const JUDGMENT_TERMS = [
  "答错了", "你错了", "扣分", "惩罚", "清零", "排名", "落后", "淘汰",
  "不可跳过", "强制", "碾压", "吊打",
];

/** 排名类正则：超过/领先 X% 的用户/学习者（模板化攀比文案） */
export const JUDGMENT_PATTERNS = [
  /超过\s*\d+\s*%/, /领先\s*\d+\s*%/, /击败\s*\d+\s*%/, /战胜\s*\d+\s*%/,
];

/** 施压/否定类：warning（人工复核），详见文件头分层说明 */
export const CAUTION_TERMS = ["错误", "失败", "必须"];

/** 规则 4：营销/引流/夸大类（无营销水文） */
export const MARKETING_TERMS = [
  "最强", "最牛", "史上最", "一学就会", "精通", "稳赚", "必看", "干货",
  "加微信", "扫码", "公众号", "私信我", "限时", "优惠", "秒杀", "爆款",
  "报名链接", "收藏起来", "关注我", "全套资料", "内部资料", "保姆级",
];

/** 规则 6：越界内容（永久剔除清单：CV 与无关领域；对齐 PRD §3.2 知识边界） */
export const OUT_OF_SCOPE_TERMS = [
  "计算机视觉", "图像识别", "目标检测", "语义分割", "实例分割",
  "区块链", "元宇宙", "NFT", "Web3", "量化交易", "炒股", "数字货币",
];
